# Learned Patterns — what actually moves the needle

*Append-only log maintained by the CEO Agent. Each entry is a pattern (on-page format × intent type) backed by **verified** outcome data from L1 (`gsc-watch-snapshot.md`) and L2 (`llm-citations` umbrella issue). The CEO reads this every run **before** triaging any new `seo-proposal`.*

## How this file is used

- **When triaging a `seo-proposal`:** check whether the proposed pattern matches a `✅ Promote` entry below. If yes, fast-track to `ai-fix`. If it matches a `❌ Do-not-promote` entry, reject with the linked reason.
- **When a new L1 verdict comes in:** append the result. Promote-pattern threshold is **2 wins in different entities for the same intent type**; do-not-promote threshold is **3 consecutive nulls for the same intent over 60+ days**.
- **When a new L2 citation pattern is detected:** append it as a Promote pattern.

Rules:

- One line per pattern (intent × format → outcome). No prose.
- Cite the L1 snapshot date or L2 issue number that produced the verdict.
- Never edit historic lines — append a counter-entry instead if a pattern stops working.

---

## ✅ Promote patterns

*(Format: `<intent type> + <on-page format> → <verdict source> · <reason>`)*

`long-tail gear-detail` + drummer profile page with structured gear inventory (brand/model per component) → L2 #2211 (2026-06-23) · Perplexity cited "what cymbals does joey jordison use" from our gear taxonomy data; specificity of component-level data matches the query granularity LLMs need for gear-detail answers. **Replicate to sibling queries:** what snare does X use, what cymbals does Y use, what bass drum does Z use.

`head-term drummer kit` + drummer profile page with gear inventory → L1 2026-06-23 snapshot · 4 entities won (brann-dailor: impr 10→22; matt-greiner: pos 6.4→5.0 + first click; aquiles-priester: impr 0→11 first appearance; mario-duplantier: pos 7.3→5.5). Structured gear inventory drives Google organic ranking for "{drummer} drum kit" head-terms. **Note:** drives organic position but NOT LLM citation (L2 gap — complement with prose kit overview per #2212).

`soundlike-guide` + HowTo + Article JSON-LD + ≥3 FAQs + internal links → pattern established from existing guides; now applying to Eloy Casagrande, Charlie Benante, Hellhammer, Aquiles Priester via #2224–#2227. First-mover advantage in black metal (Hellhammer) and neoclassical metal (Aquiles) sub-niches.

---

`lick-pages` + `/drummers/<slug>/licks/<slug>` route with specific technique name + video + notation → GA4 2026-06-24 (Igor Cavalera lick pages = 2 of top-10 GA4 pages: 9 + 6 views, outperforming all non-homepage routes). Long-form lick content drives engaged sessions (611s avg). **Replicate to:** Gene Hoglan, Dave Lombardo, Tomas Haake, Matt Greiner, Joey Jordison. Pattern confirmed: #2311 promoted + #2323 promoted (both same route format).

`comparative-list` + Top-10 drummer list page with JSON-LD ItemList schema + LLM FAQ block → L2 #2211 (2026-06-23) · "best death metal drummer", "most innovative metal drummers" — drummagazine/drumeo/loudwire winning with no metalforge.io citation. First-mover opportunity on "djent/groove/modern metal drummer" sub-niches (#2344). Replicate via Top-10 batch pages with strong opinionated prose + FAQ blocks.

`multi-variant drummer query` + comprehensive drummer profile (complete gear inventory, per-component brand/model, FAQ schema, cymbal detail) → L2 #2211 (2026-06-24) · Perplexity cited metalforge.io for 12 of 13 "joey jordison *" query variants from single /drummer/joey-jordison page ("drum set", "drum kit", "instruments", "cymbals", "drumset", "kit", etc.). One well-optimized profile page = multi-query citation sweep. **Rule: prioritize depth + completeness on high-TAM drummer profiles before spreading thin.**

`broken-relatedAlbum fix` + album-setup article that resolves 🔴 broken relatedAlbum ref in a live DB article → established 2026-06-25 (pattern: #2427/2428/2429/2477/2478/2480 all promoted as top-priority) · Fixing a broken ref is both quality fix (removes 404 from live page) and entity-cluster strengthener (LLM coherence). **Rule: any proposal flagging 🔴 broken ref on a live page is auto-5★; jumps queue ahead of pure-content proposals at same tier.**

`drummer-head-term LLM gap` + structured-only profile (gear table, no prose) → L2 #2211 (2026-06-24) · lars-ulrich/mike-portnoy/danny-carey/dave-lombardo/gene-hoglan NOT cited despite full gear inventory (moderndrummer/drumeo win with prose articles). **Contrast:** joey-jordison profile = 12/13 query variants cited (has prose + structured). **Rule: gear table alone ≠ LLM-citable. Profile must include ≥1 prose 'Kit Overview' paragraph (150-200w) using both 'drum kit' + 'drum set' phrasing to be extracted by LLM providers. Pipeline: batch 1 (#2877, shipped 2026-06-27) · batch 2 (#2968 SHIPPED 2026-06-28: Brann Dailor/Vinnie Paul/Eloy Casagrande/Shannon Larkin/Matt Halpern) · batch 3 (#2997 SHIPPED 2026-06-28: Flo Mounier/Hellhammer/John Otto) · batch 4 (#3140 queued: Danny Carey/Gene Hoglan/Tomas Haake/George Kollias) · batch 5 (#3188 queued: Ben Koller/Jaska Raatikainen/Mario Duplantier) · batch 6 (#3206 queued: Lars Ulrich/Mike Portnoy/Dave Lombardo).**

`drummer-head-term LLM gap` COMPLETE (2026-07-04) · Kit Overview prose batches 16-20 (#3681/#3682/#3687/#3688/#3689/#3690) shipped Art Cruz through Isaac Lamb, closing the roster to **62/62 (100%)** — every drummer profile now has ≥1 prose Kit Overview paragraph. **Rule update: this pattern is now baseline coverage, not a proposal source — stop filing new "add Kit Overview prose" batches. Next L2 run (#2211-successor) should re-sample the previously-uncited entities (lars-ulrich/mike-portnoy/danny-carey/dave-lombardo/gene-hoglan) to confirm prose alone converted them; if still uncited, the gap is elsewhere (schema/FAQ/depth), not missing prose.**

`album-cluster for LLM-gap drummer` + album-setup articles for a drummer absent from LLM citations → new pattern 2026-06-27 · Promoted 8 album articles for Portnoy/Lars Ulrich/Lombardo/Kollias (#2767/#2765/#2719/#2812/#2798/#2858/#2814/#2799) alongside prose fix (#2877) to create depth that reinforces LLM citation (entity cluster completeness = stronger citation signal). **Rule: for drummers in the L2 gap table, prioritize their album arc before new drummer onboarding.**

`generator-level LLM surface gap` + source data already has the field but the .md exporter never renders it → #3651 (shipped 2026-07-03) · `scripts/generate-llms-articles.cjs` never emitted the per-article `faq` array despite 383/390 album-article source files having one (sibling `generate-llms-drummers.cjs` already had the FAQ-render pattern). One generator fix retroactively fixed FAQ coverage across ~390 `/public/llms/articles/*.md` files in a single PR, directly answering 2 zero-competitor #2211 rows (Lars Ulrich/Master of Puppets, Dave Lombardo/Reign in Blood) for free. **Rule: before proposing per-entity content batches for an LLM-gap query, check whether the gap is actually a generator/exporter bug (data exists, surface doesn't) — a single infra fix beats N content batches. Grep sibling generators for the same field-render pattern first.**

`indexed share %` tracking started 2026-07-14 · L3 snapshot 2026-07-13: **74.6%** (373/500 inspected). No prior baseline to compare WoW — this is week 1 of tracking. Watch for >3pt drops as a quality-regression signal (Ralph shipping thin pages).

## ❌ Do-not-promote patterns

*(Format: `<intent type> + <on-page format> → <verdict source> · <reason>`)*

_(Empty — first entries will be appended after 60-day nulls or 3 consecutive losses.)_

---

**L3 indexed-share baseline (2026-06-29 corrected):** 106/500 = **21.2%** (2026-06-29 snapshot, 500-URL sample cap). Prior 1.4% (2026-06-26) was API quota artifact (493 "unknown"). 21.2% is the operative baseline. Flag if WoW drops >3 pts from 21.2%.

`shannon-larkin CTR gap (2026-06-26 → 2026-06-27)`: pos 4.8 / 13 impr / 0 clicks confirmed at L1 2026-06-24 snapshot. Title/meta CTR fix filed as **#2927**. Watch next L1 snapshot (2026-07-01); target ≥1 click.

`brann-dailor CTR gap (2026-06-27)`: pos 6.3 / 23 impr / 0 clicks confirmed at L1 2026-06-24 snapshot (highest-volume CTR-gap entity). Title/meta CTR fix filed as **#2928**. Watch next L1 snapshot (2026-07-01); target ≥1 click.

`joey-jordison CTR gap (2026-06-27)`: `joey jordison drum set` pos 7.7 / 149 impr / 0.67% CTR + `joey jordison drum kit` pos 8.5 / 85 impr = 234 combined impr with <2% CTR. Title/meta CTR fix filed as **#3059**. Watch next L1 snapshot (2026-07-01); target ≥2% CTR on combined cluster. Note: LLM citation is strong (12/13 variants cited) — this is a pure organic CTR gap, not a content depth gap.

`danny-carey organic position gains (2026-06-29)`: 3 big wins in single L1 snapshot — `danny carey drum kit` pos 17.0→12.3 (+impr 9→29), `danny carey drum setup` pos 15.0→11.6, `danny carey drum set` pos 18.7→15.5. All at pos 11-15 range. Driven by album-arc content merges (Lateralus indexed, Fear Inoculum + other arc articles in pipeline). **Rule: album-cluster content drives organic position gains for kit head-terms 1-2 weeks after merge, even before Kit Overview prose ships.** Kit Overview prose batch (#3140) will push Danny Carey into top-10 range once shipped.

`bill-ward new-query burst (2026-06-29)`: 4 new queries surfaced in single snapshot — `bill ward drum kit` (pos 7.0, 13 impr), `bill ward drum set` (pos 8.0, 12 impr, 2 clicks), `bill ward drum setup` (pos 7.5, 11 impr), `bill ward cymbals` (noise). All new after album-arc articles (Master of Reality + Sabbath Bloody Sabbath in pipeline). **Rule: new drummer album-arc articles generate their first organic impressions within 1-2 weeks; entry velocity correlates with band's search volume (Sabbath = fast).** Bill Ward Kit Overview prose batch (#3227, already ai-fix) will capture this traction.

`joey-jordison first-click milestone (2026-06-29)`: `joey jordison drum set` pos 7.5, 130 impr, **1 click** (was 0 clicks prior week). First organic conversion from 130-impression query. `joey jordison drumset` also 0→1 click at pos 7.7. CTR still <1% — #3059 fix must ship to convert impression volume into meaningful traffic.

`eloy-casagrande CTR gap (2026-06-29)`: `eloy casagrande drum kit` pos 9.2, 24 impr, 0.00% CTR — new L1 CTR-gap-opportunity. Title/meta fix filed as **#3282**. `eloy casagrande drum setup` also big-win (pos 13.5→8.5, ↑5.0). Eloy is current Slipknot drummer — high search-intent. Watch next snapshot; target CTR ≥2%.

`matt-greiner regression vehicle (2026-06-29)`: `matt greiner drum setup` L1 big-loss — pos 6.2→7.5, clicks 4→0. Position worsened only 1.3 points; click loss likely statistical noise at this volume. **Kit Overview prose batch #3210 (already ai-fix) is the recovery vehicle** — content depth will stabilize position and improve title relevance. No duplicate issue filed. Watch L1 2026-07-01 post-#3210 merge.

`hellhammer canonical regression (2026-06-29)`: `/articles/hellhammer-drum-setup` dropped from `indexed` to `redirect-or-canonical` (canonical → /). Probable cause: commit 21d0a907 (cross-link relatedAlbums, ~2026-06-22) introduced broken canonical logic. Filed **#3280** (urgent ai-fix). This page had established rankings — fixing canonical = recovering existing equity, not building new.

`hub-page quality floor (2026-06-29)`: `/drummers` and `/gear/cymbals` both dropped from `indexed` to `crawled-not-indexed` in L3 regression. Hub/category listing pages with no editorial prose are quality-rejected by Google. **Rule: hub pages need ≥150w editorial intro + FAQPage JSON-LD to pass Google's quality bar. A pure link list is thin content.** Filed **#3281** (ai-fix).

`L1 first-snapshot summary (2026-06-27)`: 43 new queries surfaced (first run — no prior week for win/loss classification). Joey Jordison cluster dominates: 134 impr "drum set" + 85 impr "drum kit" + 5-18 impr across 8+ variants. No big-wins or big-losses (baseline only). Next actionable snapshot: 2026-07-01.

`joey-jordison CTR fix shipped (2026-06-29 evening)`: #3059 (title/meta CTR fix) CLOSED — implemented by Ralph. `joey jordison drum set` 130 impr / 0.77% CTR fix now in production. Expect CTR lift in next L1 snapshot (2026-07-01) once re-indexed. Target ≥2% CTR.

`L1 secondary wins (2026-06-29)`: `nick augusto` impr 7→34 (pos 12.4→9.9) · `jocke wallgren` pos 17.7→11.3 · `mario duplantier cymbals` pos 12.9→7.8 — all consistent with album-arc content + profile page pattern. Confirms existing ✅ Promote rules scale to moderate-TAM entities. No new pattern entry needed.

`false-closure via commit-message keyword match (2026-07-05, root-caused)`: #1141 (critical vercel.json/api-meta routing bug, blocks JSON-LD + real titles for every AI crawler) was auto-closed as "completed" on 2026-06-16 by an *unrelated* commit (c7129a6, #1170 — Ralph drain-script eligibility fix) whose message contained the substring "(fixes #1141 case)" as a citation, not a closing intent. GitHub's keyword auto-linker matched "fixes #1141" literally regardless of surrounding words and closed it. Bug sat live and unfixed for 19 days until #3711 re-diagnosed it via fresh curl evidence. **This is the 3rd stale-closure of this exact class in one week** (Martin Axenrot SoundLike guide, #2198 comparison-pairs, now #1141). **Rule: never trust `state:closed` as proof-of-shipped — cross-check `git log --all --grep="#<N>"` for an actual merged PR before treating a closed issue as done, especially when re-diagnosing a "should already be fixed" symptom.** Also a standing risk for all agents (Ralph/CEO/SEO) writing commit messages: avoid the literal substring "fixes #N" / "closes #N" / "resolves #N" when citing an issue number for context only — GitHub does not parse intent, only the keyword+number pair.

`meta-shell routing bug survives 3 independent "fixes" (2026-07-05, live-verified)`: #1141 → #3711/PR #3718 → #3727 each closed the same underlying symptom ("crawlers get generic SPA shell, no per-page title/JSON-LD") as fixed, but production behavior never changed — confirmed by CEO's own fresh curl at 13:31 UTC showing identical etag/content-length/title/0-JSON-LD across homepage, 2 drummer pages (bot UAs), and a nonce-busted direct hit on `/api/meta/drummer/:slug` itself (`content-disposition: inline; filename="index.html"` proves Vercel resolves to the static shell before the rewrite/function ever runs). Filed as #3734 (ai-fix + priority). **Rule: a merged PR that claims to fix crawler-visible routing is NOT verified by green CI/tests alone — re-curl production with the exact bot UA and a cache-busting nonce after deploy, every time, before trusting the fix.** This bug class is the most likely root cause of #2211's 52/65 uncited L2 queries — non-JS-executing bots (GPTBot/ClaudeBot/PerplexityBot/Applebot-Extended) have plausibly never seen any JSON-LD/FAQ schema shipped in the last several weeks. Once #3734 lands, re-run the full L2 citation sweep — expect a large batch of wins, not incremental ones.

`meta-shell routing bug survives 4th independent "fix" (2026-07-05 ~14:50 UTC, live-reverified)`: #3734 → PR #3739 (merged 14:22 UTC) closed the issue but the merged diff was **only** a `Vary: User-Agent` header rule on `/drummer/:slug` — a caching directive, not a rewrite/routing change. Re-curled production ~30min post-deploy: byte-identical etag `ea0bd146...`/6849 bytes/generic title/0 JSON-LD across homepage, 2 drummer pages (bot UAs), and a nonce-busted direct hit on `/api/meta/drummer/lars-ulrich` — unchanged from #3734's original evidence. **4 consecutive "fixes" (#1141, #3711/#3718, #3727, #3734/#3739) have now failed this exact symptom**, each addressing a different (wrong) theory without re-running the issue's own verify steps before closing. Ran additional root-cause investigation this run: (1) built `packages/frontend` locally via `scripts/build.sh`'s expo export step — output is pure SPA (single `index.html`, no per-route static files), ruling out static-file-shadowing; (2) checked `public/` for shadowing `api/`/`drummer/` dirs — none found. **Rule: when a bug survives 3+ "fixes," stop trusting repo-only diagnosis — escalate to `human-founder` for anything requiring Vercel Dashboard access** (Root Directory / deployed Functions list can silently override checked-in `vercel.json` and are invisible to agents). Filed **#3742** (ai-fix, diagnostic-first instructions: add a marker response header before touching vercel.json again) and **#3743** (human-founder: check Vercel Root Directory + Functions build log). Do not promote further content-batch work as a substitute — it ships invisibly to crawlers until this lands.

`meta-shell bug: direct-hit-on-function-path also fails, ruling out routing-table gap theory (2026-07-05 ~15:30 UTC)`: a fresh `seo-proposal` (#3744) proposed the fix was "vercel.json only has 1 bot-conditioned rewrite (`/drummer/:slug`); ~13 other route families with built `api/meta/[...path].js` handlers have no rewrite path at all" — independently confirmed true (`grep -c '"has"' vercel.json` = 1; structured-data-snapshot.md shows 150/150 sampled non-drummer URLs are `no-jsonld`). But CEO's own fresh curl on `/api/meta/drummer/lars-ulrich?nonce=...` — a **direct hit on the function's own path, which requires no rewrite at all since `api/` is explicitly excluded from the catch-all** — still returned the byte-identical generic-shell etag as the homepage. **This rules out "routing table is incomplete" as the root cause**: if the function doesn't execute even when hit with zero rewrite involved, adding more rewrite rules pointing at the same function cannot fix anything. Held #3744 as `seo-proposal` (not promoted) rather than risk a 5th blind `vercel.json` edit; commented redirecting to #3742 (diagnostic marker header)/#3743 (human-founder, Vercel dashboard Root Directory check) as the correct next steps. **Rule: when a "missing config" theory is proposed for a bug that already survived multiple fixes, test the theory's own precondition first (here: does a request that doesn't need the missing config also fail?) before promoting the fix.** #3745 (genre-page dead-code migration, a genuinely independent bug unrelated to the routing theory) was promoted normally — its fix is low-risk and pays off the moment the real root cause resolves.

`meta-shell saga root-caused: production deploys are batched, not per-merge — every "false closure" today was a deploy-lag artifact, not a broken fix (2026-07-05 ~17:30 UTC)`: after 4 documented "failed fixes" (#1141, #3711/#3718, #3734/#3739, #3742/#3747) all showed byte-identical production output on re-curl, I checked the deploy pipeline itself instead of authoring a 5th repo-side theory. Found: `vercel.json` sets `git.deploymentEnabled.main = false` and `.github/workflows/deploy-prod.yml` fires production deploys **once/day at 06:00 UTC** (batched since #1797, 2026-06-19, to stop 55+ builds/day saturating Vercel's build queue) — pushing to `main` no longer triggers a build at all. `gh run list --workflow=deploy-prod.yml` showed the last actual deploy fired at **2026-07-05T07:04:21Z** — before every single one of today's routing-fix merges (14:22, 16:32, etc.). **None of today's "false closures" were ever actually deployed; my repeated re-curls were all hitting the same 07:04 UTC build regardless of what merged after it.** Tried `gh workflow run deploy-prod.yml` to deploy on-demand myself — 403, agent token lacks `actions:write`. Filed the ask on #3743 (superseding the Root Directory theory, which is now a fallback only) asking Ricardo to fire `workflow_dispatch` manually — a 10-second GitHub Actions button click, not a settings dig. **Rule: before diagnosing ANY "merged fix, unchanged production behavior" symptom on this repo, run `gh run list --workflow=deploy-prod.yml --limit 1` and compare its timestamp against the fix's merge time. If the deploy predates the merge, the fix is unverified, not disproven — do not author a new theory or spend more Ralph/CEO cycles until a fresh deploy actually ships it.** This should have been the first check in #3742, not the fourth-order root-cause dig — retroactively, at least 2 of the "failed fix" cycles (#3734/#3739, #3742/#3747) may have been correct code that simply never got a chance to prove itself.

`meta-shell saga resolved for /drummer/:slug, but the same deploy exposed a 2nd bug class (2026-07-06 09:25 UTC)`: the 2026-07-06 06:00 UTC scheduled deploy (fired 07:29:35Z) finally postdated all of 2026-07-05's routing fixes. Re-curled production: `/drummer/lars-ulrich` and `/drummer/joey-jordison` (Googlebot UA) both confirmed genuinely fixed — distinct etag, `x-meta-handler: hit-v1`, real title, JSON-LD present. The #3059 CTR title/meta fix is now visible to crawlers for the first time. Closed #3743. **But** the same deploy also shipped #3775's 16 new bot-conditioned rewrites (genre/bands/vs/technique/licks/lists/guides/battles/quotes/gear/articles) with a distinct bug: those rules point at a bare `/api/meta/<literal>` destination with no `?path=` query mapping into the `[...path].js` catch-all, so they 404 to every crawler bot — worse than before #3775 (was a 200 generic shell, now a hard 404, active deindexing risk on already-ranking pages like `/articles/hellhammer-drum-setup`). Filed **#3807** (ai-fix + priority). **Rule: any new bot-conditioned rewrite added to vercel.json must use the exact `/api/meta/[...path]?path=<literal source path>` destination format (copy the working `/drummer/:slug` rule) — a bare literal destination silently 404s instead of reaching the catch-all function. Verify every new rewrite with a bot-UA curl expecting `x-meta-handler: hit-v1`, not just a merged PR.**

`meta-shell saga's trailing L3/L1 fallout, unified diagnosis (2026-07-06 ~12:00 UTC)`: L3 snapshot (#3819, generated 10:54 UTC) showed 57 `duplicate` URLs all canonicalizing to `/drummer/jay-weinberg` + 5 new `error-404` rows. Cross-checked every affected route family against `vercel.json` — all match exactly the 16 bot-conditioned rewrites broken by #3775 and fixed by #3807/PR #3817 (merged 11:22 UTC, **after** the last production deploy at 07:29:35 UTC — not live yet). Root cause: before the fix, `/articles/:slug` and `/guides/:slug` either 404'd to Googlebot (explains the 5 error-404s — e.g. `cowboys-from-hell-drum-setup`, which has full live content in `ARTICLE_METADATA`) or served a generic SPA shell, which Google's dedup algorithm collapsed into one arbitrary "representative" URL (jay-weinberg). Also tied the L1 big-losses on `jay weinberg drum kit`/`brann dailor drum kit`/`danny carey drum setup` (all ≤10 total impressions this week) to the same fallout rather than filing 3 separate low-signal issues. **Rule: when an L3 snapshot shows a large cluster of unrelated URLs all canonicalizing to ONE arbitrary page, check whether that page's route family was recently broken/fixed in vercel.json before treating it as a fresh canonical bug — Google's own dedup clustering, not application code, picked the fake canonical.** No new ai-fix filed; watch item is re-verifying post-#3817-deploy with a bot-UA curl on `/articles/hellhammer-drum-setup`, then expect duplicate/404 counts to drop over 1-2 weeks as Google re-crawls.

`joey-jordison query-variant redistribution, not a regression (2026-07-06)`: L1 big-loss on `joey jordison drum set` (impr 130→63) and `joey jordison drum kit` (impr 86→35) despite position *improving* on both (7.5→6.7, 7.3→7.2). Not filed as a regression — several sibling JJ queries are simultaneously new/big-win this week (`joey jordison 1999` new, `joey jordison drum setup` big-win with first click), consistent with query-variant redistribution within an already-strong, already-fixed cluster rather than a real loss. **Rule: a big-loss classification on one query variant of a multi-variant cluster is not actionable in isolation — check sibling variants in the same snapshot before filing; if impressions moved sideways across the cluster while position held or improved, it's redistribution, not regression.**

`first-click conversion wave confirms mid-position CTR fixes (2026-07-06 09:29 snapshot, 7 big-wins)`: 6 of 7 big-wins this snapshot are "clicks 0→1" or "0→2" at pos 5-9 on entities with a prior shipped CTR/content fix — `bill ward drum kit` (0→2, pos 7.3), `eloy casagrande drum kit` (0→2, pos 6.7, confirms #3282's title/meta fix converted), `john otto drum kit` (0→2, pos 5.6) and `john otto drum setup` (0→1, pos 7.1), `matt greiner drum setup` (0→1, pos 6.2), `joey jordison drum setup` (0→1, pos 8.3), `bill ward drum kit 1970` (0→1, pos 6.9). **Rule: pages sitting at pos 5-9 with 0 historical clicks and an already-shipped title/meta or Kit Overview prose fix reliably convert to first clicks within 1-2 snapshots — this is now a 3rd-confirmed-entity pattern (promote threshold met). Keep prioritizing title/meta CTR fixes + Kit Overview prose for any entity stuck at pos 5-9 with 0% CTR** over net-new content for entities already ranking in this band.

`meta-shell saga chapter 6: PR #3817's textually-correct fix still 404s for 15/16 route families post-deploy (2026-07-07 07:13 UTC, live cache-busted curls)`: after confirming via `gh run list --workflow=deploy-prod.yml` that today's 06:00 UTC deploy (fired 07:08:53 UTC) was the first to include PR #3817 (merged 07-06 11:22 UTC), re-curled a spread of route families with Googlebot UA + cache-busting nonce. Result: **only `/drummer/:slug` returns `x-meta-handler: hit-v1`**; every other bot-conditioned rewrite (`articles`, `guides`, `vs`, `lists`, `technique`, `battles`, `genre`, `bands` — confirmed on 11 distinct URLs including previously-`indexed`/currently-ranking pages like `blackwater-park-drum-setup`, `death-magnetic-drum-setup`, `fastest-double-bass-drummers`) returns a genuine Vercel-platform `404 NOT_FOUND` with **no** `X-Meta-Handler` header at all — meaning the rewrite never reaches the function, ruling out a function-level bug. The failing rules are byte-identical in shape to the working `/drummer/:slug` rule (confirmed via direct read of both), ruling out a JSON typo or missing `?path=` mapping (that was #3775/#3807's bug, already fixed in the current file). Filed **#3905** (URGENT ai-fix) with a testable hypothesis: Vercel's filesystem-match-before-rewrite behavior may be triggered by stray physical directories in the Expo static export (`packages/frontend/dist/genre/`, `dist/articles/`, etc.) that don't exist for `drummer/` — untested, needs a local `scripts/build.sh` run + dist inspection to confirm. **Rule: this saga has now failed 6 independent "fix" cycles (#1141→#3711→#3727→#3734→#3742→#3775/#3807) on slightly different symptoms each time. A "byte-identical rule, one works, others don't" pattern is diagnostic gold — it rules out config-content bugs (typos, wrong query mapping) and points at something rule-position- or filesystem-dependent instead. Don't re-propose another vercel.json content tweak without first testing that specific hypothesis.** This is far more severe than prior chapters — a genuine platform 404 (not a wrong-content 200) risks actual deindexing, not just missing JSON-LD, across ~700+ non-drummer URLs. Next L1/L3 snapshots should show this as a mass regression wave if #3905 isn't resolved before the next Google crawl cycle.

`meta-shell saga RESOLVED after 6 chapters (2026-07-07 09:06 UTC, SEO Agent live audit + CEO cross-check)`: #3905 (chapter 6 — hard 404s on 15/16 bot-conditioned route families) closed same-day. A fresh bot-UA + cache-busted curl across all 8 previously-broken route families (`articles`, `guides`, `vs`, `technique`, `battles`, `bands`, `genre`, `lists`) now returns `200` + `x-meta-handler: hit-v1`, matching the working `/drummer/:slug` baseline. `git log` confirms the fix landed via commit 85111ae1 (#3807/#3817's `?path=` mapping fix, deployed via the 2026-07-07 06:00 UTC scheduled run) — no filesystem-shadowing fix was needed after all; the exotic hypothesis in #3905 was superseded once the already-correct fix actually got a chance to deploy. Closed the paired `human-founder` ask (#3906) as moot. **Total saga cost: #1141→#3711→#3727→#3734→#3742→#3775/#3807→#3905, ~7 issue cycles across 12 days; root cause was consistently deploy-lag (once/day batched deploys) plus one real content bug (#3775's missing `?path=` mapping) — never the exotic filesystem-shadowing theory.** L3/L1 fallout (57 duplicates-to-jay-weinberg, 5 error-404s, 3 big-losses on jay-weinberg/brann-dailor/danny-carey) attributed to this saga should self-heal over the next 1-2 Google crawl cycles now that the routes are genuinely fixed. **Watch the 2026-07-13 L1/L3 snapshots for confirmation; do not file new issues for the same URLs unless they're still broken by then.**

`meta-shell saga epilogue: /bpm + /bpm-tap were never in scope for any of the 7 fix cycles (2026-07-07 12:15 UTC)`: mid-day pulse cross-checked the 57 `duplicate→jay-weinberg` URLs in the 2026-07-06 L3 snapshot against the consolidated bot-rewrite regex (`vercel.json:532`, fixed by commit 85111ae1). All 57 matched route families already covered by that regex — expected to self-heal per the standing watch item. But a fresh live curl (bot UA) on `/bpm` and `/bpm-tap` found them **still serving the generic shell with zero canonical tag** — these two routes were never added to the regex alternation in any of the 7 saga chapters (#1141→#3905), because the original bot-rewrite list was built from the route-family set that had `api/meta/[...path].js` handlers *known and enumerated* at the time, and `/bpm`'s handler (`api/meta/[...path].js:3193-3214`, from #1579) was added later without updating the regex. Filed **#3931** (ai-fix) — a single-line regex addition (`|bpm|bpm-tap`), not a new instance of the saga's routing bug. **Rule: when auditing a route-coverage regex against a growing `api/meta/[...path].js` handler set, diff the two directly (`grep -oP "path === '/\\K[a-z-]+" api/meta/[...path].js` vs the regex alternation) instead of assuming "the saga fixed everything" — new single-route handlers added standalone (like #1579) are exactly the kind of thing a broad regex-consolidation fix misses.**

`FAQ-depth-gap pattern replicates to zero-competition entities (2026-07-14)`: #4593 (shipped via PR #4595, 04:12 UTC) expanded lars-ulrich/mike-portnoy/dave-lombardo/danny-carey/gene-hoglan FAQ blocks from 5-11 biography-skewed pairs to ≥9 gear-specific pairs matching joey-jordison's cited template. Live-checked george-kollias and tomas-haake the same morning — both still at only 3 FAQ pairs, identical shallow shape, and both are `_no competitor in citations_` rows in L2 (open first-mover opportunity, not a competitive fight). Filed **#4605** (ai-fix) to replicate the same expansion. **Rule: after any FAQ-depth fix ships for an L2-gap batch, immediately live-curl (`grep -o '"@type":"Question"' | wc -l`) the FAQPage question count for every OTHER profile not in that batch — shallow-FAQ is a roster-wide baseline gap (was true for lars-ulrich until today), not isolated to whichever 5 entities the snapshot happened to sample that week. Prioritize zero-competitor rows first — no authority fight needed, just ship the format.**

`joey-jordison/shannon-larkin L1 losses tied to already-shipped indexation fix, not a new regression (2026-07-14)`: L1 snapshot (2026-07-13) shows `joey jordison drum set` (pos 6.7→9.9) and `joey jordison drum kit` (pos 7.2→13.6) as big-losses. Cross-checked against L3: `/articles/joey-jordison-signature-gear-guide` was `crawled-not-indexed` (content-quality reject) and fixed via **#4559** (closed 2026-07-13T14:07, content-depth fix). Google hasn't re-crawled since (`last crawl: 2026-07-06` still shown in the 07-13 L3 snapshot for that URL). **Rule: before filing a new L1-loss issue, check L3 for a `crawled-not-indexed`/`discovered-not-indexed` row on the same entity's pages — if a fix already shipped and is just waiting on re-crawl, don't duplicate; watch the next snapshot instead.** Also checked the 26-URL `duplicate→navene-koperweis` cluster in the same L3 snapshot: all last-crawl dates are 2026-07-02 to 2026-07-04, predating the 2026-07-06/07 meta-shell-saga route fixes — same stale-crawl-data self-heal pattern documented at line 95, not a fresh bug. No issue filed; watch for the cluster to clear over the next 1-2 snapshots.

`meta-shell saga chapter 8: consolidated regex rule (from #3920) went from "works for 21 alternatives" to "works for zero" the moment #3960/#3972 added 2 more (2026-07-08 19:40 UTC, #4101)`: URGENT report claimed drummer/genre/articles all broken *simultaneously* — a bigger blast radius than any of the first 7 chapters, which always broke a strict subset of route families. Live bot-UA + cache-busted curls (including a nonce'd never-before-seen slug) confirmed: **every single alternative in the one consolidated `vercel.json:532` regex now falls through to the generic `/index.html` shell** — not just `drummer`/`gear` (the 2 alternatives #3972 added). Direct `curl /api/meta/drummer/lars-ulrich` still returns perfect Person/FAQPage JSON-LD (function is fine, matching every prior chapter's conclusion). Cross-check: a *plain, pre-existing, unrelated* `:slug`-style **headers** rule (`/drummer/:slug` → `Vary: User-Agent`, added in #3739, no regex, no `has`) is *also* silently not applying (response `vary: Accept`, no `User-Agent`), while `/api/gear/:id`-style dynamic rewrites *inside* `/api/` continue to work fine. Ruled out: (1) stale cache — nonce'd unseen URL still broken; (2) deploy lag — `gh run list --workflow=deploy-prod.yml` shows the 06:48 UTC run *did* fire after #3972 merged; (3) JS regex logic — replicated the exact `^...$` pattern in `node -e` against all 22 example paths, 100% match, so the pattern is not logically wrong; (4) stray static files shadowing the route (no expo-router, no per-route static output, `public/` has no top-level `drummer`/`genre`/etc. entries). **The evidence (any non-`/api/` dynamic rule — regex or plain `:param`, with or without `has` — silently no-ops, while `/api/*` dynamic rewrites and the single maximally-broad `/((?!api/).*)` catch-all still work) points at something in Vercel's platform-level filesystem/SPA-fallback precedence for non-API routes, not a content bug in this file** — but this could not be proven conclusively without Vercel dashboard/build-log access (same class of blocker as #3743/#3906). Applied a **defensive, low-risk mitigation regardless of the exact platform mechanism**: reverted the one-rule-to-rule-them-all consolidation from #3920 back to 22 independent per-family rewrites (simple `:param` + `has`, same style proven live in chapters 1-2 for `/drummer/:slug` alone), so that the next single-family regex edit (inevitable — this route set grows every week) can only break its own family instead of all 22 at once. **This does not fix today's incident if the true cause turns out to be platform-side** (framework-preset auto-detect overriding `framework: null`, or similar) — that still needs a human to check the Vercel dashboard the way #3743/#3906 did. **Rule: a single "1 regex to match N route families" rewrite rule is a hidden single point of failure — one bad edit or one platform hiccup takes down N families at once instead of 1. Prefer N small rewrites over 1 big one for anything crawler-visible, even though it's more boilerplate in vercel.json.** Watch item: re-curl all 5 repro URLs from #4101 after the next 06:00 UTC deploy; if still broken, the platform-side theory is confirmed and a human-founder Vercel dashboard check (Framework Preset, Root Directory) is the only remaining lever.

`meta-shell saga RESOLVED: chapter 8's watch item confirmed the 22-independent-rewrites revert (PR #4110) was sufficient — platform theory was wrong (2026-07-09 11:05 UTC)`: the 07:09 UTC deploy (first to postdate PR #4110's merge) shipped the revert. Fresh bot-UA + nonce curls across `/drummer/lars-ulrich`, `/articles/hellhammer-drum-setup`, `/genre/thrash` all returned distinct etags, `x-vercel-cache: MISS`, real per-page titles + JSON-LD, `x-meta-handler: hit-v1` — fixed without any Vercel Dashboard change. Closed the paired human-founder issue #4111 as moot. **Correction to chapter 8's own hypothesis: the evidence pattern (non-`/api/` dynamic rules silently no-op) looked platform-level, but the actual fix was purely the N-small-rewrites-over-1-big-one structural change — the single consolidated regex itself was the point of failure, not Vercel's routing precedence.** Saga closed after 8 chapters (#1141 → #4101/#4111), ~5 weeks. **Standing rule reaffirmed: never re-consolidate the per-route-family `vercel.json` rewrites back into one regex, no matter how repetitive it looks — that consolidation is what caused chapters 7 and 8.**

`album-arc article proposal (#4160) promoted on a factually wrong premise — CEO verification checked keyword presence, not band-membership tenure (2026-07-10 01:xx UTC)`: promoted #4160 (Eloy Casagrande — Kairos 2011 Sepultura album-arc article) after grepping that "Kairos" appeared once in `eloy-casagrande.js`'s career-timeline blurb. Roadie caught the real bug across 18 stopped attempts and refused to ship it: Kairos (2011) was recorded and released by Sepultura's *prior* drummer Jean Dolabella (sessions Dec 2010–Mar 2011, released June 24 2011) — Casagrande didn't join Sepultura until November 2011, five months after release, and never played on it. His actual studio debut (*The Mediator Between Head and Hands Must Be the Heart*, 2013) was already covered elsewhere. Closed #4160 as not-planned. **Rule: for any "drummer X — album Y studio-arc article" proposal, verification must confirm drummer X was an actual member of the band *during that album's recording/release window* (check a join-date fact, not just that the album title appears near the drummer's name in a data file) — a mention in a career-timeline blurb can legitimately reference a predecessor's or successor's album for context without claiming the current drummer played on it. Roadie's refusal-with-citation here is the correct failure mode; the CEO promotion step is what needs the extra check, not Roadie's judgment.**

`joey-jordison drum-kit/drum-set L1 regression (2026-07-13) — investigated as cannibalization, ruled out; new articles not yet indexed`: L1 snapshot showed 2 big-losses with no offsetting big-win in the cluster (`joey jordison drum set` pos 6.7→9.9, impr 63→33; `joey jordison drum kit` pos 7.2→13.6, impr 35→31), correlating with 2 new JJ articles merged in the prior 48h (#4502 `/articles/iowa-anniversary`, #4424 `/articles/joey-jordison-legacy`) both wired into the same gear cluster as the `/drummer/joey-jordison` profile page. Investigated as suspected keyword cannibalization per the filed issue (#4550). Ruled out on 3 independent checks: (1) the same-day `indexation-snapshot.md` (generated 10:25 UTC, ~70min after the GSC snapshot) shows **both new URLs as `URL is unknown to Google`** — Google hasn't discovered/crawled either page yet, so neither can be competing for ranking signal on anything; (2) live bot-UA curl on both new articles confirms title/meta/canonical are correctly scoped to their differentiated intent (anniversary/legacy/tribute) — neither `seoKeywords` list nor rendered `<title>` contains "drum kit"/"drum set" head-term phrasing, and both link back to `/drummer/joey-jordison` as "His Complete Kit & Gear Setup," reinforcing rather than competing with the profile page; (3) live bot-UA curl on `/drummer/joey-jordison` confirms title (`Joey Jordison Drum Set, Kit & Instruments | MetalForge`) and self-canonical are unchanged and correct. **Verdict: not cannibalization — regression is either transient SERP volatility (2 big-losses + 1 null variant, no other JJ big-loss precedent) or unrelated to the 2 new articles.** No code change shipped. **Rule: before filing/promoting a cannibalization theory for a new-article-correlated position drop, check `indexation-snapshot.md` for the new URL(s) first — if Google hasn't indexed a page yet, it cannot be cannibalizing anything, regardless of how tightly it's wired into the entity's internal-link cluster.** Watch next L1 snapshot (2026-07-20): `joey jordison drum set`/`drum kit` should recover toward the 6.7/7.2 baseline if this was noise; if the loss repeats **and** `indexation-snapshot.md` by then shows the 2 new articles as `indexed`, re-open as genuine cannibalization.

`2026-07-13 09:12/10:25 UTC L1+L3 snapshots — first fresh data in 7 days, triaged same-morning`: 5 big-wins (mike-portnoy-drum-kit pos 34→11.4, jocke-wallgren impr 8→42, bill-ward-drum-setup first click, ray-luzier-drum-setup pos 12.4→7.3, best-steel-drummers pos 49→40) — no single common cause identified across these 5 (different pages, no shared recent merge); logging as organic SERP movement, not a replicable pattern this round. 4 big-losses: 2 were joey-jordison (see above, ruled transient/no-suspect). The other 2, judged as **not worth an ai-fix this run**: (1) `shannon-larkin drum kit` pos 4.8→13.2, impr 16→12 — zero merges touched Shannon Larkin data/routes in the prior 7 days (checked `git log --since="7 days ago"`), and the page already carries a full `DRUMMER_META_OVERRIDES` entry (cited as the template match in #4551) — no code suspect exists, so an "investigate" issue would dead-end exactly like #4550 did. Treating as transient volatility per the same rule; watch the 2026-07-20 snapshot, escalate only if it repeats with a real suspect. (2) `fiafap` pos 14.7→3.0, impr 152→1 — not a recognizable phrase, no page in the codebase targets it; this reads like a one-week bot/scraper query burst, not a real user-intent regression. **Rule: a mechanically-flagged "big-loss" only warrants an ai-fix if there's an actual suspect (recent merge, code change, or repeat-week pattern) — filing "investigate" issues against losses with zero correlated changes just burns Roadie cycles reproducing the JJ null result.**

`eloy-casagrande-drum-kit CTR-gap re-flagged (2026-07-13) despite already-shipped fix (#3282, closed 2026-06-30)` — still pos 9.2, 26 impr, 0% CTR, same as the pre-fix baseline. Live-checked: title/meta/FAQPage schema from #3282's fix are still in place and match the target pattern exactly. **Not re-filed.** At 24-26 weekly impressions, 0 clicks is within noise for a page converting at a typical 2-4% CTR (expected value <1 click/week) — the sample is too small to conclude the #3282 fix failed. **Rule: don't re-fix a CTR gap that already got one title/meta intervention unless the impression volume is large enough (~50+/week) to trust CTR=0% as signal rather than noise, or unless the query shows 0% CTR across 3+ consecutive weekly snapshots.**

`26-URL "duplicate→navene-koperweis" canonical cluster (L3 2026-07-13) — stale Google index data, already fixed live` — snapshot flagged 26 unrelated URLs (`/bpm`, various `/guides/*`, `/articles/*-drum-setup`) as canonicalizing to `/drummer/navene-koperweis`. Live bot-UA curl on 3 sample URLs (`/bpm`, `/articles/blood-mountain-drum-setup`, `/guides/best-cymbals-for-progressive-metal`) shows each correctly self-canonicalizing today — no live bug. Last-crawl dates on the flagged rows are 2026-07-02/03/04, consistent with this being residual index data from a bug already fixed by earlier merges (#4148, #4380 era). Same for the `error-404` row (`dance-of-death-drum-setup`, last crawl 2026-07-06) — live curl returns 200 now. **No ai-fix filed for either; both should self-heal on Google's next recrawl.** Watch the 2026-07-20 snapshot — if either cluster is still flagged after 2 recrawl cycles, that's the signal to investigate for real (Google not revisiting vs. a live regression).

`soundlike-guide LLM-mirror false-done, 4th recurrence (2026-07-15)`: #4644 found 5/67 `public/llms/guides/how-to-sound-like-*.md` files still serving the generic HTML shell (never actually created) despite **4 prior issues** (#3612/#3674/#3763/#4296) each closed as done — every one of those PRs touched `soundLikeGuides.js`/`guides.md`/`api/sitemap.js` but never wrote the individual per-drummer `.md` file the sitemap already promised. **Rule: for any "N files across a data-driven route family" issue, the verify step must count actual files on disk (`find ... | wc -l` matching the data module's entry count), not just confirm the hub/index/sitemap references the slug — a closed issue that only updated the hub is not proof the leaf files exist.** Same failure shape as the `llms/index.md` drift class (#4519/#4543/#4298/#4342/#4571) but one layer deeper (missing leaf file, not just a missing table row).

`FAQ-depth gap is roster-wide, not batch-specific — full 67-entry count taken (2026-07-15 ~21:00 UTC)`: after #4593/#4605/#4607 shipped FAQ-depth expansion for 15 profiles, live-counted `sections.faq.items.length` across every entry in `extendedBios.js` to check whether the gap was actually closed roster-wide. Result: only 9 profiles reach the proven ≥9-pair threshold (lars-ulrich, joey-jordison, gene-hoglan, dave-lombardo, mike-portnoy, danny-carey, matt-greiner=9, tomas-haake=10, george-kollias=11). **53 profiles sit at exactly 8 items** (one short of the threshold) and **7 are far shallower at 5-6 items, 100% biography-skewed with zero gear-specific pairs** (eloy-casagrande, mario-duplantier, martin-axenrot, isaac-lamb, sean-reinert, adrian-erlandsson @5; nick-menza @6). Filed 3 batches respecting the 3-issue-per-run L1/L2/L3 cap: **#4701** (7 shallowest), **#4702**/**#4703** (10 each of the 8-item group, ~33 remain queued for future runs). **Rule: when a content-depth fix ships for N entities out of a roster of M, always live-count the full roster before assuming the pattern is closed — a "batch shipped" issue title does not mean the underlying metric (FAQ count, prose presence, schema field) was checked on the untouched majority.** Reuse this count script each future run to track batch progress: `node -e "import('./packages/frontend/data/extendedBios.js').then(m => Object.entries(m.extendedBios).forEach(([s,v]) => console.log((v.sections?.faq?.items||[]).length, s)))"`.

`FAQ-depth-gap roster sweep CLOSED — all 67 profiles now ≥9 items (2026-07-17 07:38 UTC)`: live re-count (`extendedBios.js`) after #4701/#4702/#4703 shipped shows distribution 50@9 / 16@10 / 1@11 — zero profiles remain below the proven ≥9-pair threshold, closing the gap first identified 2026-07-15 (53@8, 7@5-6). Hub-level FAQPage-pairing (companion pattern) also closed same day: #4809/#4810 (5+2 hubs) and #4816/#4817 (21 hubs + `/vs`) together cover every top-level hub identified lacking `BreadcrumbList`/`FAQPage`. **Rule: both roster-wide content-depth levers from the 07-15 sweep are now fully drained — do not re-propose FAQ-depth or hub-schema-pairing batches without a fresh gap-count showing regression. Watch the 2026-07-20 L1/L2 snapshots to see whether this cluster of fixes actually moved citations (8/84 baseline) or position/CTR before crediting the pattern.** Next replicable-format search should look elsewhere (e.g. Kit Overview prose depth, per-entity Speakable coverage) rather than re-running these two.

`kevin-talley L1 big-loss (2026-08-10, #5492) — two-week impression spike reverting to baseline, not a regression`: `kevin talley` flagged big-loss (pos 10.7→14.0, impr 61→3). Full 6-week history from `.agents/seo/gsc-history/*.json`: impr 1 (07-06) → 4 (07-13) → 5 (07-20) → **42 (07-27) → 61 (08-03)** → 3 (08-10) — the 08-03 baseline the loss is measured against was itself the spike's peak, not a stable level; 3 impressions this week is back in line with the pre-spike 1/4/5 range. Sibling long-tail query `kevin talley drummer` moved the same direction the same weeks (12→16→2 impr), ruling out a page-specific template/content regression (a real regression on his profile page wouldn't also suppress a different query string in lockstep). Checked `gh pr list --search kevin` for a culprit merge: the most recent PR touching any kevin-talley data file is #4210 (2026-07-10, endorsement timelines) — over a month before the spike even started, so no code change correlates with either the rise or the fall. Matches the already-documented `fiafap` one-week-burst pattern (line 121) and the CEO's own same-day triage in `.agents/seo-plan.md` ("Not filed"). **No ai-fix shipped. Rule: when a big-loss's "last week" baseline is itself an outlier spike relative to the preceding 2-3 weeks (check the full `gsc-history/*.json` series, not just the 2-week snapshot diff), classify as spike-reversion noise, not regression — especially when a sibling query variant moved the same way with no differentiating cause.** Watch: if `kevin talley` stays ≤5 impr in the 2026-08-17 snapshot, confirmed noise (no further action); if it re-spikes above ~40 or the position keeps sliding past 14, escalate with a fresh look at SERP competitors.

## 📦 Archived watches

*(Queries removed from `watched-queries.json` because they showed 3 consecutive null verdicts. CEO appends here when archiving instead of letting them rot in the live watch list.)*

_(Empty.)_

---

*Last reset: 2026-06-23 (file seeded — no historical patterns yet).*

`sitemap diet + L3 measurement overhaul (2026-07-16, PRs #4867/#4868, founder-commissioned)`: the "74.6% indexed" L3 headline was a fixed top-500-priority sample re-inspected every week — rotation was promised in the script comment but never implemented, so 90%+ of the 6,523-URL sitemap had never been inspected once. Two-thirds of that sitemap was ballast: 2,211 all-pairs /vs/ comparisons (2 of 335 impression-earning queries) and ~1.9K /llms/**.md mirrors (for AI crawlers, but inviting Google to crawl duplicate markdown). Fixed by (a) sitemap diet to ~2,800 URLs — /llms/ excluded wholesale, /vs/ gated by VS_DEMAND_DRUMMERS (grow only from GSC evidence); (b) real rotation: 250 fixed sentinels + 250 rotating with persisted cursor (~11-run full cycle); (c) earning-pages proxy (GSC by-page, 90d) as the true full-site indexation score. **Rules: (1) combinatorial page families (all-pairs, all-combos) are banned without a demand gate — 34% of the sitemap for ~0.7% of demand is the cautionary number; (2) never re-add /llms/**.md to the sitemap — llms.txt is their discovery surface; (3) any sampled metric must state its sample everywhere it is reported — the unqualified "74.6%" hid the blind spot for weeks; (4) the earning-pages number is the site-wide truth, inspection classes are the per-URL diagnosis — don't conflate them.**

`L2 stagnation root-caused (2026-07-16): wins correlate with being the only good source, not with schema` — all 8 cited queries (of 84, Perplexity-only) are niche entities where our page is the best on the internet (eloy-casagrande, matt-greiner, mario-duplantier, hellhammer, lateralus article); losses are head terms held by authority sites or queries where Perplexity cites no gear site at all. **Rule: stop expecting FAQ/schema refinements to move L2 on contested queries — L2 grows by (a) creating citable facts that exist nowhere else (drum-chair histories, verified BPMs, computed studies) and (b) domain authority. Also: the 8/84 denominator contains known junk (fiafap, blake-richardson-actor) pending target cleanup — do not treat the ratio as clean signal until docs/seo-l2-l3-recovery.md open item #3 ships.**

`L2 minimum-pressure trigger cleared (2026-07-23)`: the 2026-07-20 llm-citations refresh (#2211) shows **33/97 cited**, crossing back above the 25-count floor that triggered the "≥2 forced pattern-level L2 issues/week" rule (added 2026-07-14 at 8/84). **Rule: the forced-L2-pressure quota is currently NOT active — resume normal L1/L2/L3-cap triage. Re-arm it automatically if a future snapshot drops the cited count back below 25.**

`mid-tier drummer queries oscillate week-to-week with no code suspect (2026-07-23)`: `mike-portnoy-drum-kit` and `jocke-wallgren` were both **big wins** in the 2026-07-13 snapshot (portnoy pos 34→11.4; wallgren impr 8→42) with no identified cause then either — logged as "organic SERP movement, not a replicable pattern." The 2026-07-20 snapshot shows both **reverting** almost exactly to their pre-win baseline (portnoy pos 11.4→15.3, impr 59→4; wallgren pos 11.4→14.5, impr 42→8), alongside `bill-ward-drum-setup`/`drum-set` and `mario-duplantier-drum-kit` also dropping. Checked live: all 4 pages already carry the fully-optimized `DRUMMER_META_OVERRIDES`-class title/meta (or equivalent "Drum Kit & Gear Setup" template); `git log` shows zero merges touching any of the 4 in the prior 7 days. No ai-fix filed — this is the same "no actual suspect" dead-end #4550 (Joey Jordison) and the 2026-07-13 Shannon Larkin note already established. **Rule: once an entity has shown one win→loss (or loss→win) cycle with zero correlated code change, add it to a mental do-not-file watchlist — a 2nd or 3rd oscillation on the same query with still-no-suspect is confirmation of noise, not escalating evidence of a real regression.**

`/tools hub missed the #4355 ssrLinks sweep (2026-07-23)`: #4355 added crawlable `ssrLinks` to 9 hub pages but not `/tools` — live curl confirmed `/tools`'s bot-facing shell links to nothing but itself + favicon, while `/tools/compare` and `/tools/metal-drummer-name-generator` sit `discovered-not-indexed` in the current L3 snapshot. Filed #4925. **Before assuming a discovered-not-indexed cluster needs new internal links, curl the specific hub bot-shell first** — in the same pass, `/guides` and `/articles` hubs turned out to already link their sampled discovered-not-indexed pages (mathcore/deathcore guide slugs, 3 sampled orphan articles all present in the shell nav), so those are crawl-budget/patience, not a linking bug — no issue filed for them, avoided a speculative fix.

`danny-carey-kit CTR gap crosses the 3-week persistence threshold (2026-08-03)`: `danny carey kit` climbed pos 11.0→8.9→8.6 across the 07-20/07-27/08-03 snapshots (top-10 now) but held at exactly 0 clicks all 3 weeks (26/22/20 impr) — checked via `.agents/seo/gsc-history/*.json`, not just the latest snapshot, per the eloy-casagrande rule (2026-07-13 entry) that requires 3+ consecutive 0%-CTR weeks before treating a low-volume CTR-gap as signal rather than noise. Unlike eloy-casagrande, this query had **no prior title/meta intervention** (danny-carey's only prior fix, #4739, targeted the `drum set` cluster, not bare `kit`) — filed as a fresh gap, #5214, scoped to `metaDescription` only since `metaTitle` already contains "Drum Kit". **Rule: when checking a CTR-gap-opportunity row, always pull the 3-week history from `gsc-history/*.json` before deciding noise vs. signal — the current snapshot alone can't show persistence.** Watch the 08-10/08-17 refreshes for ≥1 click.

`danny-carey-kit CTR gap fix shipped, #5214 (2026-08-03)`: rewrote `metaDescription` only (title unchanged, already correct). Before: `'Complete biography of Danny Carey, Tool\'s legendary drummer known for complex polyrhythms, sacred geometry, and innovative drumming. Explore his career, philosophy, and gear.'` — biography framing, no answer to a gear-lookup query. After: `'See what drum kit Danny Carey (Tool) plays — Sonor SQ2 Heavy Beech kit, Sonor Danny Carey Signature bronze snare, Paiste Signature cymbals, and Mandala electronic pads. Complete gear guide.'` — matched the shape of the two entries in this file already proven to convert kit/gear-lookup intent (`shannon-larkin`: "See what drum kit Shannon Larkin (Godsmack) plays — ..."; `eloy-casagrande`: "Full breakdown of Eloy Casagrande's drum kit as Slipknot's drummer: ..."), both leading with the answer (specific gear brands/models) instead of biography. Gear specs pulled from the verified `gear` object in `packages/backend/src/index.js` (Sonor SQ2 Heavy Beech, Sonor Danny Carey Signature 14x8" Bronze snare, Paiste Signature Series, Mandala Drum electronic pads) — no new facts invented. **Verdict pending — watch the 08-10/08-17 `gsc-watch-snapshot.md` refreshes for `danny carey kit` moving off 0% CTR; if still 0% after 2 more snapshots at this position/impression level, treat as a description-shape miss rather than re-trying noise.**

`L2 41/100 vs 43/100 (2026-08-03)`: `.agents/seo/learned-patterns.md` has no history file for L2 citations (only the latest `#2211` issue body survives), so a 2-point week-over-week dip can't be attributed to a specific query flip without re-running the check manually. Read as Perplexity API non-determinism, not a regression — still far above the 25-count floor. **If a future refresh shows a multi-week downward trend (not just one dip), that's the signal to start manually diffing the "Not cited" table against the prior week's issue body (via `gh issue view --json body` history isn't kept either — would need to snapshot it) rather than assuming noise again.**

`gearHighlights-vs-FAQ contradiction is a systemic extendedBios.js data-rot class, not isolated incidents (2026-08-07)`: SEO Agent has now surfaced and shipped this exact bug shape for **19 of 72 profiles** (eloy-casagrande, mario-duplantier, scott-travis, hannes-grossmann, flo-mounier, derek-roddy, dirk-verbeuren, paul-bostaph, matt-garstka, jaska-raatikainen, abe-cunningham, daniel-erlandsson, jason-bittner, raymond-herrera, morgan-ågren, blake-richardson, martin-lopez, matt-greiner, joey-jordison) across ~30 merged PRs in 3 days — each profile's hand-authored `gearHighlights.content` prose (and sometimes `sources.items`) cites an old/wrong brand that the profile's own `faq` array and the site's source-of-truth gear modules (`snares.js`/`cymbalSetups.js`/`pedals.js`/`api/drummers/index.js`) already contradict correctly. Root cause: `gearHighlights` prose was hand-written once and never re-synced when gear facts were later corrected elsewhere — a duplicate-of-truth problem (CLAUDE.md rule #1 in spirit, even though `gearHighlights` isn't a separate *data module*, it's un-derived prose duplicating fields that live elsewhere). **Rule: auto-fast-track any `seo-proposal` matching this exact shape (gearHighlights/sources prose contradicts the same entry's FAQ + a cited source-of-truth gear file) — verified-only, single-file, zero-new-surface, freeze-compliant by construction. ~53 profiles remain unaudited; keep promoting as SEO Agent surfaces them rather than batching, since each is independently verifiable and low-risk.** Long-term fix (not proposed yet, flagging for later): generate `gearHighlights.content` from the same structured gear data (`snares.js`/`cymbalSetups.js`/`pedals.js`) instead of hand-authored prose, which would make this entire bug class structurally impossible — worth a founder-ideas entry once the current trickle exhausts itself.

`kevin-talley bare-name L1 big-loss (2026-08-10) investigated and ruled unexplained SERP volatility, not a regression (#5492)`: `kevin talley` fell pos 10.7→14.0 / impr 61→3 in the 08-03→08-10 snapshot, the largest impression drop of the week's 3 big-losses. Checked `git log` across `extendedBios.js`, `snares.js`, `pedals.js`, `drumsticks.js`, `drummerComparisons.js`, `gearIndex.js`, and `packages/frontend/pages`/`components` for the full 08-01→08-11 window — zero commits touch `kevin-talley` (last kevin-talley content merge was #4180, 2026-07-10, a month prior) and zero merges touch any shared/roster/comparison template that could dilute the bare-name query. Pulled the full 5-week history from `.agents/seo/gsc-history/*.json` for both `kevin talley` (impr 4→5→42→61→3) and the correlated `kevin talley drummer` variant (impr 1→2→12→16→2, pos 9.25→11.5) — **both queries spiked together in the 07-27/08-03 weeks and collapsed together in 08-10, in the same proportion (~87-95% impression drop)**, with no page-specific change on either query's landing page. Same signature as the `mike-portnoy-drum-kit`/`jocke-wallgren` win→loss cycle (2026-07-23 entry above): a 2-week impression spike of unknown external origin, then reversion toward the pre-spike baseline. **Rule: when a bare-name (no gear/kit modifier) query big-loss has (a) no correlated code change and (b) a modifier-variant of the same entity's name moving in the same direction/proportion in the same window, that's confirmation of head-term SERP volatility (or an external event driving a temporary spike) rather than a site regression — do not file a content fix, and do not re-investigate from scratch if it recurs without a fresh suspect.** Watch the 08-17 `gsc-watch-snapshot.md`: if `kevin talley` recovers toward the ~10-15 impr pre-spike baseline (not the 61-impr spike), that confirms noise; if it stays at 3 impr / pos 14+ for 2+ more weeks with still no suspect, that's a genuine floor-drop worth a fresh look (title/meta/schema audit on `/drummer/kevin-talley`), not a re-run of this same dead-end.

`eloy-casagrande-slipknot / adrian-erlandsson L1 big-losses ruled noise, not regression (2026-08-14, 08-10 snapshot)`: both flagged big-loss in the 2026-08-10 `gsc-watch-snapshot.md` (`eloy casagrande slipknot drum kit` pos 3.8→8.0; `adrian erlandsson` pos 17.6→23.0). Full 6-week `gsc-history/*.json` pull shows both queries have been oscillating in the same band for months before this week's reading — eloy-slipknot: pos 5.8→3.0→10.25→6.875→3.8→8.0 (bounced 3-10 the whole time); adrian: pos 20.5→23.9→17.6→23.0 (bounced 17-24 the whole time) — and **zero clicks in every single week regardless of position** for both. `git log` across the full window shows no commits touching either drummer's data or shared templates. Same signature as the kevin-talley/mike-portnoy/jocke-wallgren oscillation entries above. **Rule: extend the oscillation-noise pattern — a big-loss on a query with 6+ weeks of position swinging ±5-7 and 0% CTR at every position (win or loss) is definitionally not costing clicks; don't file a content fix, the position swing itself is the steady state.** No ai-fix filed.

`L3 duplicate-canonical pair confirmed stale-crawl artifact, not a live bug (2026-08-14)`: the 2026-08-10 `indexation-snapshot.md` flagged `/bpm` and `/guides/best-drum-hardware-for-metal` as `duplicate` (Google-selected canonical → `/lists/math-metal-drummers`), both with `Last crawl` dates of 2026-07-02/07-03. Live Googlebot-UA curl today confirms both pages currently serve correct self-referential `<link rel="canonical">` tags. `/bpm`'s bot-detection rewrite gap was fixed by #3934, merged 2026-07-07 — **4 days after** the stale crawl date the snapshot is keying off. **Rule: before filing an L3 `duplicate`/`crawled-not-indexed` issue, check the row's `Last crawl` date against `gh pr list` / `git log` for the target route — if a rewrite/canonical fix already merged after that date, live-curl (with a real Googlebot/GPTBot UA) to confirm before filing; the snapshot can lag a shipped fix by multiple weekly cycles until Google re-inspects.** No ai-fix filed; watch the 08-17 snapshot for these 2 URLs to drop out once Google recrawls.

`FAQPage JSON-LD present but invisible as body text is a systemic api/meta/[...path].js class, not isolated (2026-08-13)`: `generateMetaHtml()`'s visible-FAQ body block and `generateFaqSchema()`'s JSON-LD emission both key off a top-level `meta.faqSchema` field. Several route branches never set that field — instead they hand-serialize their own `articleSchema` string with a `FAQPage` node embedded directly inside its `@graph`. Net effect: valid FAQPage JSON-LD ships, but non-JS crawlers (GPTBot/ClaudeBot/PerplexityBot/Applebot-Extended) see zero visible Q&A text, only the schema. Surfaced and shipped for 56 route branches already (#5478), then this run alone added `/drummer/<slug>` flagship profiles (#5520, 72p, merged via #5523), `/gear/<brand>/<series>/drummers-using` (#5521, ~40p), `/lists/<slug>` (#5522, 98p — 3 of which are open L2 #2211 rows), and a 10-route-family batch found via full `FAQPage`-occurrence grep-audit (#5524: drummers/licks hub 72p, top10 articles 12p, /compare 12p, /brands 16p, /genre 7p, /quiz, /drummers hub, /tools, +more). Fix pattern proven safe: hoist the already-computed Q&A array into a local var, reuse it both inside the existing `articleSchema` FAQPage node (unchanged) and set a new `meta.faqDisplayItems` field that `generateMetaHtml()` renders as visible body text with **no JSON-LD** (avoids a duplicate-FAQPage regression). **Rule: auto-fast-track any `seo-proposal` matching this shape (grep confirms a live `FAQPage` JSON-LD node on a route whose return object never sets `meta.faqSchema`) — additive-only, zero new pages, directly serves the L2/depth-work freeze priority since it makes existing Q&A content citable to LLM crawlers that wasn't before.** Once #5524's route list is exhausted, do a final grep sweep of `api/meta/[...path].js` for any remaining `'@type': 'FAQPage'` occurrence not paired with `meta.faqSchema`/`meta.faqDisplayItems` — treat a clean sweep as closing this bug class.

`FAQPage sweep completed — bug class fully closed on all LIVE routes (2026-08-15)`: ran the final grep sweep promised above. Of 28 `'@type': 'FAQPage'` occurrences in `api/meta/[...path].js`, 21 are correctly paired with `faqDisplayItems`/`faqSchema`. Of the 7 remaining: 3 are the `/gear/<brand>/<series>/drummers-using` branches already covered by the open, untriaged `#5521` (don't re-file); 2 are the `/drummer/<slug>/licks` **singular** sub-branches, which `#5524`'s body already ruled out as unreachable dead code (no route matcher, no sitemap entry — reachable only by accident via a generic bot-UA rewrite); 1 is the bare-slug `/{slug}` drummer-profile branch (`drummerMatch`, ~line 3917), already ruled dead code in `.agents/seo-plan.md` line 4680 (`curl https://metalforge.io/danny-carey` returns the plain SPA shell, no vercel.json rewrite serves this pattern in production) — not a duplicate finding, just confirming the prior ruling still holds. **Rule: this bug class is now closed for every real, reachable URL on the site. Do not re-grep `api/meta/[...path].js` for FAQPage occurrences again — the only remaining hits are the 3 `#5521`-pending branches (drop once that ships) and permanently-dead code that will never be fixed (not worth re-verifying each run).**

`gearHighlights-vs-FAQ and sources.items sibling-field-miss classes: full-roster audit complete, both closed (2026-08-15)`: dispatched agents to audit the last unaudited ground for both classes — the 16 of 72 `extendedBios.js` profiles never yet touched by a gearHighlights-vs-FAQ fix (lars-ulrich, dave-lombardo, tomas-haake, george-kollias, martin-axenrot, gavin-harrison, john-otto, jay-weinberg, charlie-benante, art-cruz, nick-menza, jimmy-degrasso, nick-barker, alex-rudinger, john-longstreth, waltteri-vayrynen), and the ~44 already-gearHighlights-fixed profiles whose `sources.items` field hadn't yet been checked for the sibling-field-miss pattern (see #5465/#5481 lineage). **Zero new contradictions found in either sweep** — both bug classes are now fully exhausted across all 72 profiles. Also swept the `trivia` field (never systematically audited before) across all 72 profiles for the same staleness shape: found exactly one genuine case, matt-greiner (`trivia` line 7200 + `keywords` line 7111 still cite pre-2016 Pearl/Meinl instead of current Mapex/Paiste — filed as `#5569`), no others. **Rule: do not re-run any of these three sweeps from scratch again — `gearHighlights`/`sources.items`/`trivia` staleness in `extendedBios.js` is now a fully-mined, closed vein. Any future finding in these fields will be a fresh regression (a NEW edit introducing drift), not backlog — investigate it as such (check recent `git log` on the specific slug) rather than re-auditing the whole roster.** Long-term structural fix (generate `gearHighlights.content` from `snares.js`/`cymbalSetups.js`/`pedals.js` instead of hand-authored prose) remains unproposed — worth a founder-ideas entry if a fresh regression appears, since it would make the whole class structurally impossible.

`albumArticles/*.js vs extendedBios.js gear-brand contradiction — new vein confirmed, 8 more found (2026-08-17)`: continuing #5693/#5694's discovery (brand contradictions live in `albumArticles/` too, not just `gearHighlights`/`sources.items`), this run's SEO Agent output surfaced 8 more (#5708-5715: matt-greiner, flo-mounier, inferno, raymond-herrera, pete-sandoval, derek-roddy, travis-orbin, blake-richardson) — all independently verified live against `extendedBios.js`'s `gearHighlights.content` before promoting (all 8 confirmed genuine, one needed external tama.com/sabian.com verification since blake-richardson's albumArticles file was internally consistent but uniformly wrong across all 8 dated entries). **Rule: `albumArticles/*.js` is now a confirmed second front for the same brand-drift bug class as `extendedBios.js` — do not assume the 2026-08-15 "fully-mined, closed vein" note (gearHighlights/sources.items/trivia) covers this file family too; it doesn't, this is a separate, still-open vein. Keep auto-fast-tracking as SEO Agent surfaces them.**

`L2 continues climbing: 49/100 cited (2026-08-17 refresh, up from 43/100 mentioned in CLAUDE.md's last strategic-state note, up from 8/84 baseline)`: minimum-pressure rule (CEO-AGENT.md, "while cited count <25/84") no longer triggers — comfortably clear of the floor. Two investigations this run on "not cited" rows came back as **authority gaps, not content/rendering defects** (see next two entries) — a signal that the easy rendering-bug harvest in this table may be thinning out; future L2 work may need to lean more on the data-studies/backlink play (CLAUDE.md's top strategic priority) than more page-level fixes.

`band-drummer fact-lookup queries ("who is the drummer of X") investigated, ruled authority-gap not rendering bug (2026-08-17)`: L2 #2211 shows `slipknot`/`tool`/`gojira`/`mastodon`/`pantera` drummer-identity queries uncited (drummerworld/drumeo/moderndrummer/blabbermouth win). Dispatched an agent to check for a #5478-class visible-body-text bug on `/bands/<slug>` — found none: live PerplexityBot-UA curl confirms the FAQ ("Who is the drummer for X?") already renders as visible `<h3>/<p>` body text with correct, current answers, and titles/descriptions already name the drummer near the top. Only soft gaps found: no standalone Person microdata block (drummer profiles have one, band pages don't) and thinner overall content (300-354 words vs 600+ on drummer pages). **Rule: not confidently actionable as a single-pattern code fix — logging as investigated/inconclusive per the L2 minimum-pressure rule's "silence is not acceptable" clause. Do not re-file a rendering-bug hypothesis for this pattern without new evidence; if revisited, treat as a content-depth/authority question (more prose, Person microdata) not a bug hunt.**

`blast-beat technique queries investigated, ruled authority-gap not content-gap (2026-08-17)`: L2 #2211 shows `what is a blast beat`/`types of blast beats`/`blast beat technique metal drummers` uncited (drummagazine/drumeo win). Live-curled `/techniques/blast-beat` — page already directly answers all 3 query shapes in visible FAQ text (definition, BPM range, 5 named variations) at reasonable length (218 words). No rendering defect found. **Rule: same conclusion as the band-drummer entry above — established competitor sites (drumeo, drummagazine) likely win on backlink authority for generic technique fact-lookups, not content gaps we can close with an SSR fix. No ai-fix filed; consistent with CLAUDE.md's standing view that backlink/authority work, not more page-level fixes, is the remaining lever for these generic/high-competition queries.**

`ranked-list item highlight/reason text found JSON-LD-only, same bug class as the closed FAQPage sweep — filed #5721 (2026-08-17)`: while investigating the comparative-list L2 gap (`fastest metal drummer`/`best death metal drummer`/`most innovative metal drummers`/`thrash metal drummers ranked` — same 3 competitors winning all 4), found `/lists/<slug>`'s hand-curated `rankings[id].highlight`/`.reason` text (`top10Lists.js`) reaches the `ItemList` JSON-LD's `item.description` field (`api/meta/[...path].js` ~4175-4189) but never the visible body — the page's only visible content beyond FAQ is a bare drummer-name list (confirmed via PerplexityBot-UA curl of `/lists/fastest-metal-drummers`: 790 total words, zero per-drummer reasoning text). Exact same shape as the closed FAQPage-JSON-LD-only bug class (2026-08-13/08-15 entries) but for ranked-list descriptions instead of FAQ. Filed **#5721**: extend `ssrLinks` entries with the existing `highlight`/`reason` text, render additively in `generateMetaHtml()` (must not break the many other route families sharing the plain `{href, label}` ssrLinks shape). **Rule: the FAQPage-only sweep was scoped to FAQ specifically — this shows the same "rich content computed, JSON-LD-only, never visible" failure mode can recur in OTHER schema types (ItemList, and potentially Person/Review/etc.) on the same file. Worth a follow-up grep sweep of `api/meta/[...path].js` for other `itemListElement`/structured fields with rich `description`/`highlight` text that similarly never reach `ssrLinks` or another visible-body path, once #5721 ships and proves the fix pattern.**

`CTR-gap trio (mario-duplantier/crystal-mountain/eloy-casagrande drum kit) held as low-volume noise, not re-filed a 5th time (2026-08-17)`: all 3 flagged again in the 08-17 `gsc-watch-snapshot.md` CTR-gap table despite 4 prior rounds of title/meta fixes (#3282, #4623, #5493, #5692). Pulled 6-week `gsc-history/*.json`: both mario-duplantier and eloy-casagrande DO convert clicks intermittently (eloy: 0,0,1,2,1,0 clicks across 07-13→08-17; mario: 0,0,0,2,0,0) at low impression volume (12-51/week) and mid position (8-14) — not a dead 0%-forever pattern, just expected variance at this volume where 1 click swings CTR several points. **Rule: extend the noise-threshold rule — a CTR-gap row on a query that has shown ≥1 click within the last 4-5 snapshots at similar volume/position is oscillating-noise, not a fresh regression needing a 5th fix attempt. Only re-open if a query goes fully 0% for 4+ consecutive weeks AFTER already having converted at least once post-fix (that would indicate the fix regressed, not just noise).** No ai-fix filed.

`08-17 snapshot wins reconfirm the pos-5-9-first-click pattern (2026-08-18)`: 3 of 5 big-wins this snapshot are "clicks 0→1" at pos 7-13 on entities with prior shipped CTR/content fixes (`brann dailor drum kit` pos 7.6, `danny carey drumkit` pos 9.1, `danny carey drum setup` pos 12.7) — same shape as the line-101 pattern (promote threshold already met, no new action needed, just further confirmation). Other 2 wins (`best cymbal set for metal` pos 10.8→7.0, `joey jordison drum kit` impr 23→61) are organic SERP movement with no code suspect, consistent with the line-151 oscillation note. No new ai-fix filed.

`extendedBios.js is not an automatic source-of-truth when 2+ other files agree against it, or the claim is career-spanning — do an external tiebreak (2026-08-18)`: while working the queued albumArticles.js gear-contradiction batch, aquiles-priester's case broke the usual assumption (that `extendedBios.js` gearHighlights is always correct and `albumArticles.js`/other files are the stale ones). Found `drummerEvolution.js` independently *agreed* with `albumArticles.js` (both: current = Trick Drums/Ufip/DW 9000) against `extendedBios.js`'s claim (Pearl Reference/Sabian) — 2 files vs 1. External check (his own official website, aquilespriester.com/site/setup-2022) revealed BOTH internal claims were wrong; real current gear is Mapex Saturn Evolution + Paiste, matching neither. A third file (`drummerComparisons.js`) had yet a third fabricated variant. Filed as one 5-file fix, #5755. Separately, frost's `albumArticles.js` general-setup entry claimed Pearl "throughout his career" against extendedBios.js's Tama — external check (tama.com official artist page) confirmed extendedBios.js was right this time, filed as #5762. **Rule: before treating an `albumArticles.js`/`drummerComparisons.js`/`drummerEvolution.js` vs `extendedBios.js` gear disagreement as a simple one-sided fix, grep 1-2 more sibling data files (drummerComparisons.js, drummerEvolution.js, soundLikeGuides.js) for the same drummer's gear claims. If any of them agree with the "wrong" side against extendedBios.js, or the claim is asserted as true for the drummer's entire career (not just one album/era), do one external search/fetch (official band/drummer site, or brand's own artist page) before deciding which side to fix — don't assume extendedBios.js wins by default.**

`my-own-summer-shove-it BPM page flagged as big-loss (2026-08-18), held — not a 3rd fix attempt`: `my own summer bpm` appears as the sole big-loss row in the 08-17 `gsc-watch-snapshot.md` (impr 72→35, clicks 0→0, pos 9.4→9.3 — position actually flat/slightly better). This is the same page as #5493 (title fix) and #5692 (metaDescription fix, closed as still-0%-CTR) — 2 prior rounds already spent on it. The drop is impression *volume*, not position or CTR, with clicks unchanged at 0 both weeks — reads as query-demand seasonality (a song-specific BPM search), not a page-quality regression a 3rd content fix could reach. **Rule: extend the CTR-gap-trio precedent — a big-loss row that is really an impression-volume dip (position flat/improved, clicks unchanged) on a page that already absorbed 2 fix rounds is not worth a 3rd. Only re-open if position also regresses ≥3 or clicks that were previously >0 drop to 0.** No ai-fix filed.

`bundle already over perf-budget.yml's budget before the coffee-button PR (2026-08-19)`: measured while validating #5835 (Buy Me a Coffee button, StickyCTA.js) — built baseline main via `bash scripts/build.sh`, measured `index-*.js` + `__common-*.js` raw bytes = 5,210,232, against `perf-budgets.json`'s `budgetRawBytes: 5,141,545` — **already ~69KB over budget on main, before any change in that PR** (which itself added a measured +1,352 bytes, negligible and unrelated to the overage). Not investigated further in that session — flagging for whoever picks this up. **Rule: perf-budget.yml's CI gate should have caught this on whichever PR pushed the bundle over budget, but the overage exists on main regardless — check recent bundle-affecting merges (the bands/songs/studies epics landed large data modules around this period) for the actual cause, and either trim/lazy-load or do an explicit, reviewed budget bump per perf-budgets.json's own instructions ("Bump budgetRawBytes/warnRawBytes only as an explicit, reviewed change alongside a note on why the bundle grew"). Don't let this recur silently — CEO: verify perf-budget.yml is actually failing PRs right now, not just warning.**

`joey-jordison-drum-kit content-gap row is a first-click confirmation, not a fresh gap — held, not a 5th CTR fix (2026-08-24)`: metrics.md's mechanical content-gap filter (impr≥50, CTR<2%) flagged `joey jordison drum kit` (57 impr, 1.75% CTR, pos 9.8) this run. Pulled `gsc-history/*.json` for the exact query across 4 snapshots: 08-03 (20 impr, 0 clicks), 08-10 (23 impr, 0 clicks), 08-17 (61 impr, 0 clicks, logged as a "win" on impression growth alone) — then this run's live data shows the first click in that run (1/57 ≈ 1.75%) with position also improving 10.8→9.8. This lands right after two very recent, unrelated data-accuracy fixes on this exact drummer (#5819 gear-fabrication fix closed 08-19, #5995 technique-guide misstatement fix closed 08-21) — consistent with the line-101 pos-5-9-first-click pattern, not a stagnant CTR problem. Query already absorbed 4 dedicated title/meta CTR rounds historically (#2544, #2867, #3059, #3412). **Rule: extend the pos-5-9-first-click pattern to explicitly cover the mechanical content-gap filter too — when a impr≥50/CTR<2% row is actually a query converting its first click in 3+ snapshots (not a dead 0%-forever), that's a leading indicator a recent fix is working, not a gap needing a 5th near-duplicate CTR fix. Only re-open if the next snapshot reverts to 0 clicks at growing/flat impressions.** No ai-fix filed.
