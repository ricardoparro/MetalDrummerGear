# Memory - 2026-03-12

## Session Summary

### Completed Tasks
- **Mobile keyboard fix deployed** - Added `inputMode="text"`, `enterKeyHint="search"`, touch attributes to native `<input>` search bar
- Commit `d251670` pushed to production
- Awaiting Ricardo's confirmation that mobile keyboard works

### In Progress: Articles Index Page (`/articles/`)
- Added `isArticlesIndexPage()` function at line ~14976 in App.js
- Route pattern: `/articles/` or `/articles` → index page, `/articles/:slug` → individual articles
- Next: Create `ArticlesIndexPage` component after line ~2392 (where TopListPage ends)
- Need to add `showArticlesIndex` state near line 18573
- Include SEO meta tags and Schema.org CollectionPage markup
- List album articles from `./data/albumArticles` + top 10 lists with `isArticle: true`

### Blocked Items
- **Vercel KV**: Needs dashboard config (Storage → Create Database → KV → Connect)
- **Twitter API** (#528): Ricardo needs to create developer app
- **Morgan Ågren videos**: Video data source unknown - not in drummer bios

### Key Technical Context
- **CSS fix**: `packages/frontend/fixCssStyles.js` (MUST be first import in App.js)
- **SearchBar**: Uses native HTML `<input>` to bypass react-native-web proxy issues
- **Vote API**: `api/battles/vote.js` uses `@vercel/kv` for persistence
- **Articles data fix**: Line ~1651: `(list.drummerIds || (list.drummerId ? [list.drummerId] : []))`
- **Branch**: `feature/640-most-popular-gear`

### Agent Updates
- Social agent prompt updated to completely ignore FCK.FM (never mention, even if they engage)
- @MetalDrumGear Twitter: 60 posts, 5 followers, 22 following

### Pending Goals
1. Confirm mobile keyboard fix works
2. Complete `/articles/` index page
3. Configure Vercel KV for battle votes
4. Find video data for Morgan Ågren
5. Complete #519 spacing tokens
6. Resume News Aggregation (#510-#514)
