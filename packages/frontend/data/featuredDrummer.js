// Featured Drummer rotation configuration (Issue #494)
// Curated weekly rotation with birthday overrides

// The featured rotation schedule - drummers featured by week number
export const FEATURED_ROTATION = [];

// Get featured drummer for the current date
// Returns null for now as the feature is in development
export function getFeaturedDrummer(drummers) {
  return null;
}

// Get upcoming spotlight birthdays (within next 7 days)
// Returns empty array for now
export function getUpcomingSpotlightBirthdays(drummers) {
  return [];
}

// Get the schedule of upcoming featured drummers
// Returns empty array for now
export function getFeaturedSchedule(drummers) {
  return [];
}
